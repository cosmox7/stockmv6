function checkAppLock(password) {
    // Correct validation logic
    const validPassword = "correct_password"; // Replace with actual password or validation logic
    if (password === validPassword) {
        return true; // Password is valid
    }
    return false; // Password is invalid
}